---
layout: post
title: "Building a CUDA GEMM Kernel from Scratch: Chasing cuBLAS on an A100"
date: 2026-03-01
category: "CUDA · GPU Architecture · Performance Engineering"
summary: "Progressive optimization of a CUDA GEMM kernel from naive baseline to 72% cuBLAS throughput on an A100, then extended to a fused multi-head attention kernel achieving 11.29 TFLOPS with DRAM throughput reduced to 0.62% — verified via Nsight Compute roofline analysis."
description: "CUDA GEMM kernel optimization to 72% cuBLAS throughput, extended to fused multi-head attention with online softmax eliminating O(N²) HBM traffic, profiled with Nsight Compute on A100."
tags: [CUDA C++, NVCC, Nsight Compute, A100 GPU, cuBLAS, Roofline Analysis, FlashAttention]
links:
  - label: GitHub
    url: https://github.com/LinLinHua/CUDA-GEMM-Kernel-Optimization
    external: true
---

This project implements a single-precision General Matrix Multiplication (GEMM) kernel in CUDA C++ from scratch, progressively optimizing from a naive baseline through shared memory tiling, register blocking, float4 vectorization, and systematic parameter tuning. The GEMM kernel was then extended to a fused multi-head attention kernel implementing online softmax to eliminate O(N²) HBM traffic. All kernels were profiled on an NVIDIA A100 GPU using NVIDIA Nsight Compute. The final tuned GEMM kernel achieved **13.75 TFLOPS — 72% of cuBLAS throughput** for a 4096×4096×4096 matrix multiply. The fused attention kernel achieved **11.29 TFLOPS at N=4096** with DRAM throughput reduced to **0.62%**, verified via roofline analysis.

---

## Background: GEMM, the Roofline Model, and GPU Memory Hierarchy

GEMM ($C = A \times B$) is the foundational operation of deep learning — every linear layer, every attention head, every convolution expressed as im2col reduces to matrix multiplication. cuBLAS achieves near-peak throughput through assembly-level optimizations and hardware-specific tuning, but building a high-performance kernel from first principles is the most direct path to understanding GPU architecture [1].

The **roofline model** [2] provides the theoretical framework for GPU performance analysis. Every kernel operates somewhere in the space defined by two limits: the peak memory bandwidth (bytes/second) and the peak arithmetic throughput (FLOPS/second). A kernel's position is determined by its **arithmetic intensity** — the ratio of floating-point operations to bytes loaded from memory (FLOP/byte). Kernels below the ridge point are memory-bound; those above are compute-bound. The A100 has peak fp32 throughput of 19.5 TFLOPS and HBM2e bandwidth of ~2 TB/s.

The GPU memory hierarchy creates a large performance gap between on-chip and off-chip memory:

| Level | Bandwidth | Latency | Scope |
|---|---|---|---|
| HBM2e (global) | ~2 TB/s | ~500 cycles | All SMs |
| L2 Cache | ~12 TB/s | ~200 cycles | All SMs |
| Shared Memory (SRAM) | ~20 TB/s | ~20 cycles | Per SM |
| Registers | ~80 TB/s | ~1 cycle | Per thread |

Warp-level execution is the key to understanding GPU throughput. A warp is a group of 32 threads that execute in lockstep. When a warp stalls waiting for memory, the scheduler switches to another eligible warp to hide the latency. **Occupancy** — the ratio of active warps to the hardware maximum — determines how much latency hiding is available. High register usage per thread limits occupancy, creating a fundamental trade-off between per-thread arithmetic intensity and warp-level latency hiding [3].

---

## Optimization Journey

### Stage 1 — Naive Baseline

The naive kernel assigns one output element per thread. Each thread independently iterates over the K dimension, reading directly from global memory (HBM) on every step with no data reuse whatsoever.

```cuda
// One thread → one output element
int row = blockIdx.y * blockDim.y + threadIdx.y;
int col = blockIdx.x * blockDim.x + threadIdx.x;
float sum = 0.0f;
for (int k = 0; k < K; k++)
    sum += A[row * K + k] * B[k * N + col];
C[row * N + col] = sum;
```

Nsight Compute immediately confirms the diagnosis: memory throughput near 70%, compute throughput near 14%, and SM utilization almost zero. The kernel is **deeply memory-bound** — it does almost no arithmetic per byte loaded.

**Result: 2.40 TFLOPS (4096×4096) — 12.6% of cuBLAS**

---

### Stage 2 — Shared Memory Tiling

The standard fix for memory-bound GEMM is shared memory tiling [1, 4]. A thread block cooperatively loads a BK-wide tile of A and B from HBM into shared memory (`sA`, `sB`), then all threads compute their partial dot products locally before loading the next tile. Each float loaded from HBM now contributes to `BM × BN` multiply-adds rather than one, multiplying arithmetic intensity by the tile size.

```
Arithmetic intensity per tile:
  Compute:    BM × BN × BK × 2  FLOP
  Load:      (BM × BK + BK × BN) × 4  bytes
  Intensity = (BM × BN × BK × 2) / ((BM + BN) × BK × 4)
            ≈ BM/8  FLOP/byte  (for BM = BN)
```

For BM = BN = 64, intensity ≈ 8 FLOP/byte — already 50× better than naive.

**Result: 5.33 TFLOPS — 2.2× over naive**

---

### Stage 3 — Register Blocking

Shared memory tiling alone still has each thread computing one output element. Register blocking has each thread compute a `TM × TN` submatrix (we use TM = TN = 8), accumulating into register arrays `regC[TM][TN]`. The outer product loop loads one column of A and one row of B from shared memory into registers (`regA[TM]`, `regB[TN]`), then computes 64 multiply-adds without touching shared memory again.

```cuda
// Each thread owns 8×8 = 64 output elements
float regC[TM][TN] = {0.0f};

for (int kk = 0; kk < BK; kk++) {
    float regA[TM], regB[TN];
    for (int m = 0; m < TM; m++) regA[m] = sA[threadRow*TM + m][kk];
    for (int n = 0; n < TN; n++) regB[n] = sB[kk][threadCol*TN + n];
    for (int m = 0; m < TM; m++)
        for (int n = 0; n < TN; n++)
            regC[m][n] += regA[m] * regB[n];
}
```

The key insight: `regA` and `regB` are each loaded once from shared memory per `kk` step, but participate in `TM × TN = 64` multiply-adds. Arithmetic intensity at the shared memory level doubles again.

The cost is register pressure: `regC[8][8]` consumes 64 registers per thread, limiting theoretical occupancy to 25% on A100 (4 active warps per scheduler vs. the hardware maximum of 16).

**Result: 10.23 TFLOPS — 4.3× over naive**

---

### Stage 4 — Float4 Vectorized Loads

Replacing scalar `float` global memory accesses with `float4` (128-bit) loads and stores maximizes the HBM2e bus utilization. Each load instruction now moves 4 floats instead of 1, reducing instruction count by 4× for the same data transfer.

```cuda
// Load 4 consecutive floats in one 128-bit transaction
float4 tmp = reinterpret_cast<const float4*>(&A[globalRow * K + globalCol])[0];
sA[row][col]     = tmp.x;
sA[row][col + 1] = tmp.y;
sA[row][col + 2] = tmp.z;
sA[row][col + 3] = tmp.w;
```

The stride loop iterates in units of `float4` groups (`i < (BM * BK) / 4`), with each thread responsible for loading non-overlapping groups across the tile. Boundary conditions fall back to scalar loads.

**Result: 9.97 TFLOPS — 4.2× over naive**

<figure>
  <img src="/assets/img/posts/nsight_gpu_throughput.png" alt="GPU Speed of Light — 04_gemm_float4">
  <figcaption>Nsight Compute GPU Speed of Light for 04_gemm_float4: compute throughput 14.07%, memory throughput 69.71%. The kernel is clearly memory-bound — the compute units are starved while waiting for HBM data. This is the baseline profile before tuning.</figcaption>
</figure>

<figure>
  <img src="/assets/img/posts/nsight_roofline.png" alt="Roofline chart — 04_gemm_float4">
  <figcaption>Floating Point Operations Roofline (fp32) on A100 for 04_gemm_float4. The kernel sits in the memory-bound region, achieving 64% of fp32 peak. Increasing BK will push it rightward toward the compute-bound regime.</figcaption>
</figure>

---

### Stage 5 — Systematic Parameter Tuning

With the core optimization structure in place, the final gains come from tuning two parameters that directly control the trade-offs identified in the profile: **BK size** (which governs how much K-dimension work is done per tile load) and **shared memory padding** (which eliminates bank conflicts).

#### 5a — Tuning BK: Reducing Synchronization Overhead

The outer loop over the K dimension iterates `K / BK` times, and each iteration ends with `__syncthreads()` — a barrier that serializes all threads in the block. Every synchronization is dead time.

Doubling BK from 8 to 16 halves the number of `__syncthreads()` calls from `K/8 = 512` to `K/16 = 256` for K = 4096. It also means each float4 load stride loop runs more iterations per thread (`(BM × BK) / 4 / totalThreads` increases), which amortizes the loop overhead over more useful work.

However, BK cannot grow unboundedly. Shared memory per block scales as:

```
sA = BM × (BK + padding) × 4 bytes
sB = BK × (BN + padding) × 4 bytes

BK=8:  sA + sB ≈ 5 KB   → 32 blocks/SM resident (theoretical)
BK=16: sA + sB ≈ 9 KB   → 18 blocks/SM resident
BK=32: sA + sB ≈ 17 KB  → 9  blocks/SM resident
```

At BK=32, the increased shared memory footprint reduces the number of resident blocks per SM, hurting occupancy and erasing the synchronization gains.

| BK | Sync calls (K=4096) | Shared Mem | 4096 TFLOPS |
|---|---|---|---|
| 8  | 512 | ~5 KB  | 9.97  |
| 16 | 256 | ~9 KB  | 13.52 |
| 32 | 128 | ~17 KB | 10.99 |

**BK = 16 is the sweet spot** — synchronization overhead is cut in half without significantly degrading occupancy.

#### 5b — Tuning Padding: Eliminating Bank Conflicts

Shared memory on NVIDIA GPUs is organized into 32 banks, each 4 bytes wide. Address `a` maps to bank `a % 32`. When multiple threads in a warp access the same bank simultaneously, their requests serialize — a **bank conflict**.

In a tiled GEMM, the outer product loop reads `sA[threadRow * TM + m][kk]` for `m = 0..TM-1`. All threads in a column read the same column `kk` of `sA`, stepping down rows. With BK=16 and no padding, row `r` of `sA` starts at address `r × BK = r × 16`, mapping to bank `(r × 16) % 32`. Rows 0 and 2 both start at bank 0, rows 1 and 3 at bank 16 — only two distinct starting banks for all 64 rows, causing heavy conflicts.

Adding `padding` extra floats per row shifts each row's starting address:

```
Row r starts at bank: (r × (BK + padding)) % 32

BK=16, padding=0: row 0→bank 0, row 1→bank 16, row 2→bank 0 ...  (2-way conflict)
BK=16, padding=1: row 0→bank 0, row 1→bank 17, row 2→bank 2 ...  (distributed, better)
BK=16, padding=2: row 0→bank 0, row 1→bank 18, row 2→bank 4 ...  (evenly distributed)
BK=16, padding=4: row 0→bank 0, row 1→bank 20, row 2→bank 8 ...  (partially distributed)
```

Padding=2 makes consecutive rows land on distinct banks modulo 32, nearly eliminating conflicts for this tile geometry.

<figure>
  <img src="/assets/img/posts/nsight_memory.png" alt="Memory workload analysis — bank conflicts">
  <figcaption>Memory workload analysis from Nsight Compute. The "Shared Load Bank Conflicts" section shows 50% bank conflict rate in 04_gemm_float4 (no padding), dropping to ~20% after padding=2. The L1 Wavefronts Shared Excessive metric directly measures serialized shared memory transactions.</figcaption>
</figure>

Tuning results (M=N=K=4096):

| Config | TFLOPS | vs cuBLAS |
|---|---|---|
| BK=8,  pad=0 (baseline) | 9.97  | 52.4% |
| BK=16, pad=1            | 9.56  | 50.2% |
| BK=16, pad=2            | 13.52 | 71.0% |
| BK=16, pad=2, lb=2      | **13.75** | **72.2%** |
| BK=32, pad=4            | 10.99 | 57.8% |
| cuBLAS (reference)      | 19.04 | 100%  |

**BK=16, padding=2, `__launch_bounds__(64, 2)` achieves 13.75 TFLOPS — the best result in this project.**

<figure>
  <img src="/assets/img/posts/nsight_occupancy.png" alt="Occupancy analysis — BK16 pad2">
  <figcaption>Occupancy analysis for the best kernel (BK=16, pad=2): theoretical occupancy 25%, achieved occupancy 25.97%, limited by register count. The 4.00 theoretical warps per scheduler means latency hiding is limited — an unavoidable consequence of register blocking's register pressure.</figcaption>
</figure>

<figure>
  <img src="/assets/img/posts/nsight_warp.png" alt="Warp scheduler stats — BK16 pad2">
  <figcaption>Warp scheduler statistics for BK=16, pad=2: active warps/scheduler 3.73, No Eligible 62.22%, issue slot utilization 47.78%. Compared to 04_gemm_float4 (No Eligible 71%), the tuned kernel keeps warps eligible more often — a direct result of fewer sync barriers and reduced bank conflict stalls.</figcaption>
</figure>

---

## Full Performance Summary

All results are 10-run averages after a warmup pass. Benchmarks run on NVIDIA A100, sm_80.

| Implementation | 1024 (TFLOPS) | 2048 (TFLOPS) | 4096 (TFLOPS) | vs cuBLAS |
|---|---|---|---|---|
| 01_gemm_naive              | 2.43  | 2.79  | 2.40  | 12.6% |
| 02_gemm_tiled              | 3.99  | 4.76  | 5.33  | 28.0% |
| 03_gemm_regblock           | 4.47  | 6.40  | 10.23 | 53.7% |
| 04_gemm_float4             | 6.38  | 6.66  | 9.97  | 52.4% |
| 06_gemm_float4_ultimate    | 6.77  | 8.82  | 11.36 | 59.7% |
| **tuning/BK16_pad2_lb2**   | **7.38** | **8.57** | **13.75** | **72.2%** |
| 05_gemm_cublas             | 14.54 | 13.77 | 19.04 | 100%  |

Notable: `03_gemm_regblock` (10.23 TFLOPS) slightly outperforms `04_gemm_float4` (9.97 TFLOPS) at 4096 — this is a reminder that vectorized loads help mainly when the kernel is bandwidth-bound; at BM=128 with no padding, the float4 kernel hits bank conflict stalls that offset the vectorization gains. The padding added in later stages resolves this. `06_gemm_float4_ultimate` correctly outperforms `04_gemm_float4` after applying consistent padding across all stages.

---

## Nsight Compute Profile: Before and After

| Metric | 04_gemm_float4 | BK16_pad2_lb2 (best) |
|---|---|---|
| Compute Throughput | 14.07% | 74.03% |
| Memory Throughput | 69.71% | 62.71% |
| fp32 Peak Achieved | 52%    | **72%** |
| Achieved Occupancy | 33.79% | 25.97% |
| Bank Conflicts (shared) | 50% | ~20% |
| Bottleneck | Memory-bound | Balanced |

The transition from 04 to BK16_pad2 shifts the kernel from deeply memory-bound (14% compute, 70% memory) to a balanced operating point (74% compute, 63% memory) — the correct target state for a well-optimized GEMM kernel.

---

## What's Left: The Gap to cuBLAS

The remaining 28% gap between the best custom kernel (13.75 TFLOPS) and cuBLAS (19.04 TFLOPS) is attributable to three factors, in approximate order of impact:

**1. No latency hiding from low occupancy.** With only ~26% occupancy (~8 active warps per SM), there are insufficient warps to cover the ~500-cycle global memory latency. The long-scoreboard stall rate of 37.0% in the profile directly reflects this. *Fix: double buffering — overlap the next tile's global load with the current tile's computation using two alternating shared memory buffers.*

**2. Residual bank conflicts.** Even with padding=2, ~20% of shared memory transactions are still serialized. The root cause is that the outer product loop reads `sA` and `sB` in patterns that are difficult to fully de-conflict without restructuring the thread layout. *Fix: warp swizzling or transposing one of the shared memory tiles.*

**3. No Tensor Core utilization.** cuBLAS uses WMMA (Warp Matrix Multiply-Accumulate) instructions that execute 16×16×16 matrix multiply in a single instruction at dramatically higher throughput. All custom kernels here use scalar FP32 FMAs. *Fix: restructure to `wmma::mma_sync` — a fundamentally different kernel design.*

---

## Extension: Fused Multi-Head Attention

The GEMM optimization work established the core techniques — tiling, register blocking, float4 loads, bank conflict elimination — that directly transfer to the harder problem of fused attention. Standard (naive) attention materializes an N×N score matrix to HBM three times:

```
Step 1: S = QKᵀ / √d   → write N×N to HBM
Step 2: P = softmax(S) → read + write N×N to HBM
Step 3: O = P @ V      → read N×N from HBM
```

For N=4096 in FP32, that is ~128 MB of intermediate HBM traffic per head per forward pass. FlashAttention (Dao et al., NeurIPS 2022) [6] eliminates this by fusing all three steps into a single kernel using online softmax, keeping the score matrix in registers and SRAM.

### Online Softmax

The key insight is that softmax can be computed in a single streaming pass without storing the full row. For each KV tile, maintain running statistics:

```
m_new = max(m_old, tile_max)
alpha = exp(m_old - m_new)          ← rescale factor
l = l * alpha + tile_sum_exp
O = O * alpha + P_tile @ V_tile     ← rescale and accumulate
```

The correction factor `alpha` adjusts all previous contributions whenever the running max increases, so the final result is mathematically identical to standard softmax.

### Implementation

The fused attention kernel extends the GEMM register blocking structure directly:

```
Grid  : (H, ceil(N/Br))    — one block per head per query tile
Block : 64 threads          — (Br/TM) × (Bc/TN) = 8×8

for each KV tile:
    load sK[Bc][D], sV[Bc][D] into SRAM via float4
    GEMM-1: regS[TM][TN] = sQ @ sK^T    ← register blocking
    warp shuffle reduce: row max and sum across 8 threads/row
    online softmax update: m, l, regO
    GEMM-2: regO += regP @ sV            ← register blocking
write O to HBM once
```

The warp shuffle reduction collects the partial row max/sum from the 8 threads that share each query row, without going through shared memory.

### Results

| | N=1024 | N=2048 | N=4096 |
|---|---|---|---|
| Our fused kernel | 6.83 | 9.68 | **11.29** |
| PyTorch naive | 17.68 | 17.00 | 18.11 |
| PyTorch SDPA (FlashAttention) | 41.90 | 77.10 | 108.15 |

At N=4096, our kernel reaches 62% of PyTorch naive and scales correctly (time ∝ N²).

### Nsight Compute Verification

The critical result is not the TFLOPS number but the HBM traffic reduction:

| Metric | Value |
|---|---|
| Compute (SM) Throughput | 47% |
| Memory Throughput | 64% |
| **DRAM Throughput** | **0.62%** |
| L1/TEX Throughput | 80% |

DRAM throughput at 0.62% confirms the N×N score matrix never touches HBM. The kernel is L1/TEX bound — the bottleneck has shifted from off-chip memory bandwidth to on-chip cache and shared memory, exactly the regime shift FlashAttention targets.

The roofline shows the kernel operating at high arithmetic intensity (~700 FLOP/byte measured against L1), consistent with the register-blocking structure keeping most data in registers across the KV loop.

### Gap to PyTorch SDPA

The remaining gap to PyTorch SDPA (~10× at N=4096) is primarily:

1. **FP16 vs FP32.** PyTorch SDPA runs FP16, doubling compute throughput on A100 Tensor Cores. Our kernel uses FP32 scalar FMAs.
2. **Tensor Core utilization.** SDPA uses WMMA instructions; our kernel does not.
3. **Larger tile sizes.** Production FlashAttention uses Br=Bc=128 with carefully tuned occupancy; our Br=64, Bc=32 leaves SM utilization lower.

---

## References

[1] S. Ryoo et al., "Optimization principles and application performance evaluation of a multithreaded GPU using CUDA," in *PPoPP 2008*.

[2] S. Williams, A. Waterman, and D. Patterson, "Roofline: An Insightful Visual Performance Model for Multicore Architectures," *Communications of the ACM*, vol. 52, no. 4, pp. 65–76, 2009.

[3] NVIDIA Corporation, *CUDA C++ Programming Guide*, version 12.x, 2024. https://docs.nvidia.com/cuda/cuda-c-programming-guide/

[4] V. Volkov and J. W. Demmel, "Benchmarking GPUs to tune dense linear algebra," in *SC 2008*.

[5] NVIDIA Corporation, *Nsight Compute User Guide*, 2024. https://docs.nvidia.com/nsight-compute/

[6] T. Dao, D. Fu, S. Ermon, A. Rudra, and C. Ré, "FlashAttention: Fast and Memory-Efficient Exact Attention with IO-Awareness," *NeurIPS 2022*. https://arxiv.org/abs/2205.14135
